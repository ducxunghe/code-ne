// import HttpUtils from '~/utilities/http.util';

export const useCommonService = () => {
	// const url = `${process.env.API_URL}`;

	// const doUploadSingleImage = (file: any): Promise<any> => {
	// 	const fd = new FormData();

	// 	fd.append('image', file, file.name);

	// 	return HttpUtils.doUpload(url, fd);
	// };

	return {
		// doUploadSingleImage,
	};
};
